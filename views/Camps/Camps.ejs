<%- include('../partials/head') %>
<link rel="stylesheet" href="camps.css">
</head>
    <body>
        <%- include('../partials/navbar4') %>
        
        <%- include('../partials/flash') %>
            <div class="container">
                <div>
                    <div class="list-group camps " style="width:auto"></ul>
                    <% for(let camp of allcamps){ %>
                    <div class="card d-flex justify-content-center  m-3" style="width: auto;">
                        <div class="card-body ">
                          <h5 class="card-title"><%= camp.name %></h5>
                          <p class="card-text"><%= camp.description %></p>
                          <a href="/camps/<%=camp.id %>" class="btn btn-primary">Organisation Details</a>
                        </div>
                      </div>
                        <% } %>
                </div>
                </div>
                <br>
            </div>
           

    </body>
    <%- include('../partials/footer') %>